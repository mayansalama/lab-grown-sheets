from setuptools import setup

setup(name='lab-grown-sheets',
      version='0.1',
      description='Various helpful tools',
      packages=['labgrownsheets'],
      install_requires=['networkx>=1.11'],
      zip_safe=False)
